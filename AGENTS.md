# AGENTS.md — ハーネス・ブートストラップ仕様

このファイルはプロジェクトルートに置く。Claude Code / Codex はセッション開始時にこれを読む。

> Claude Code でも読ませる場合: `ln -s AGENTS.md CLAUDE.md`

---

## 0. 起動判定

セッション開始時、最初に `.harness/state.md` の存在を確認する。

| 条件 | モード |
|---|---|
| `.harness/` が無い | **ブートストラップモード** → 第1章へ |
| `.harness/state.md` がある | **再開モード** → 第6章へ |

ブートストラップモードでは、ユーザーが何を言っても**いきなりコードを書かない**。
「〇〇のWebアプリを作りたい」のような一行の依頼は、第1章のヒアリング開始の合図として扱う。

**このモードでの推奨メインモデル: Opus 5（既定）／ 仕様が曖昧・探索的な場合は Fable 5.1**

---

## 1. フェーズ1 — ヒアリング

### 1.1 進め方

- 1メッセージにつき **最大5問**。まとめて20問投げない。
- 各問に**推奨デフォルト案を添える**。ユーザーが「おまかせ」と言えるようにする。
- ユーザーの回答から自明に埋まる項目は**質問しない**。
- 3ラウンド以内に終える。それ以上かかるなら、未決事項を「後で決める」として記録し先へ進む。

### 1.2 ラウンド1（形態を問わず必須）

1. **何を解決するものか** — 主な利用者と、その人が今困っていること
2. **形態** — Webアプリ / モバイルアプリ / CLI / デスクトップ / ライブラリ・SDK / バッチ・データ処理 / API単体 / 拡張機能
3. **利用者の規模と性質** — 自分だけ / 社内 / 一般公開。認証は要るか
4. **データの性質** — 個人情報・決済・機微情報を扱うか（扱うなら第5章の監査ゲートが必須になる）
5. **完成の定義** — 最初のリリースで「これができていれば成功」と言える1〜3項目

### 1.3 ラウンド2（形態別に追加）

ラウンド1の回答を見て、該当する項目だけ聞く。

- **Web** — SSR/SPAの別、対応ブラウザ、SEO要否、デプロイ先
- **デザイン資産（形態を問わず、UIがあるなら必ず聞く）** — 参照画像・カンプ・既存デザインシステムの有無。無い場合はトーンの希望（2〜3案を提示）。**「見た目にどこまでこだわるか」を V0/V1/V2（4.3.1節）で確認する**
- **モバイル** — iOS/Android/両方、ストア配信の有無、ビルド環境（macOSの有無）、オフライン動作
- **CLI** — 配布形態、対話型かパイプ前提か、対応OS
- **ライブラリ** — 公開先、対応バージョン範囲、後方互換の厳しさ
- **バッチ/データ** — データ量、実行頻度、冪等性の要求、失敗時のリカバリ
- **API** — 認証方式、レート制限、公開範囲、バージョニング方針

### 1.4 ラウンド3（技術と運用）

1. **言語・フレームワーク** — 指定があるか、無ければこちらから2案提示して選んでもらう
2. **既存資産** — 流用するコード・DB・設計書があるか
3. **リポジトリ運用** — PR必須か直接コミットか、ブランチ戦略
4. **CI** — 既にあるか、新規に用意するか、実行したいチェック
5. **制約** — 期限、予算、使えないサービス、社内規定

### 1.5 ヒアリング完了条件

以下がすべて埋まったら次フェーズへ。

- [ ] 形態が確定している
- [ ] 完成の定義が1〜3項目で書ける
- [ ] 言語・フレームワークが確定している
- [ ] 機微データの有無が確定している
- [ ] リポジトリ運用方針が確定している

確定内容をユーザーに箇条書きで提示し、**明示的な承認を得てから**フェーズ2へ進む。

---

## 2. フェーズ2 — 合意文書の生成

承認後、以下を生成する。**ここまでは実装コードを1行も書かない。**

```
docs/
  product.md      # 何を作るか。利用者、課題、完成の定義。80行以内
  architecture.md # 構成、技術選定とその理由、ディレクトリ方針。150行以内
  constraints.md  # 機微データ、法令・セキュリティ要件、非機能要件。80行以内
```

`constraints.md` には、第5章の監査ゲートで機械的にチェックできる形で書く。
悪い例:「セキュリティに配慮する」／ 良い例:「パスワードは Argon2id でハッシュ化する。平文ログ出力を禁止する」

---

## 3. フェーズ3 — ハーネス生成

以下を生成する。

```
.harness/
  state.md           # 現在地。80行以内。毎タスク更新（追記でなく書き換え）
  decisions.md       # 決定ログ。1決定=1行。200行超でarchiveへ退避
  backlog.md         # タスク一覧とステータス。1タスク=1行
  tasks/T-001.md     # タスク詳細。150行以内。完了後archiveへ移動
  archive/
.claude/agents/      # Claude Code用サブエージェント定義（第4章）
```

Codex を使う場合、`.claude/agents/` はモデル自動切替としては機能しない。第4章の表を**役割定義ドキュメント**として使い、モデル切替はセッション単位で手動で行う（第4.4節）。

---

## 4. 役割とモデル

### 4.1 メインエージェント（指示役・設計者）

**モデル: Opus 5（`opus`）／ 仕様が曖昧で探索が必要なら Fable 5.1（`claude-fable-5-1`）**

責務:
- タスク分解と難易度判定（4.3節）
- サブエージェントへの委譲と成果物の受け取り
- レビューNG3回時の裁定（第5章）
- `state.md` `decisions.md` `backlog.md` の更新

**メインエージェントは実装コードを書かない。** 書きたくなったら、それは分解が足りていない合図。

### 4.2 サブエージェント一覧

| 役割 | モデル | tools | 主な仕事 |
|---|---|---|---|
| `designer` | `opus` | Read, Grep, Glob, Write | 詳細設計、インターフェース定義、タスク分解案 |
| `impl-l0` | `haiku` | Read, Write, Edit, Bash, Glob, Grep | 定型作業（4.3節 L0） |
| `impl-l1` | `sonnet` | 同上 | 仕様確定済みの単機能（L1） |
| `impl-l2` | `sonnet` | 同上 | 複数ファイル横断・中規模リファクタ（L2） |
| `impl-l3` | `opus` | 同上 | アーキテクチャに触れる実装（L3） |
| `impl-l4` | `claude-fable-5-1` | 同上 | 探索的・長時間の実装（L4） |
| `impl-ui` | `claude-fable-5-1` | 同上 | 視覚忠実度を要求するUI実装（4.3.1節 V2） |
| `ui-reviewer` | `claude-fable-5-1` | Read, Grep, Glob, Bash | 参照画像と実装結果の視覚的な突合（5.4節） |
| `test-designer` | `opus` | Read, Grep, Glob, Write | テスト観点の設計（境界値、異常系、並行性） |
| `test-writer` | `sonnet` | Read, Write, Edit, Bash, Glob, Grep | 観点に沿ったテストコード生成 |
| `auditor` | `opus` | Read, Grep, Glob, WebSearch, WebFetch | セキュリティ・法令適合の監査（第5.3節） |
| `reviewer` | `opus` | Read, Grep, Glob, Bash | レビュー判定（第5章）。**書き込み権限を与えない** |

フロントマター例:

```markdown
---
name: impl-l2
description: 複数ファイルにまたがる中規模の実装・リファクタを担当する。タスク難易度L2で使う。
tools: Read, Write, Edit, Bash, Glob, Grep
model: sonnet
---
あなたは実装担当です。渡されたタスクファイル（.harness/tasks/T-xxx.md）の
受入条件だけを満たしてください。それ以外の改善提案は実装せず、報告に留めること。
完了時は変更ファイル一覧と受入条件の充足状況を返す。
```

### 4.3 難易度判定

メインエージェントはタスクごとに以下で判定し、`tasks/T-xxx.md` に記録する。

| L | 判定基準 | 委譲先 |
|---|---|---|
| L0 | 機械的。判断が要らない。ボイラープレート、DTO、一括リネーム、フォーマット | `impl-l0` |
| L1 | 仕様が確定。既存パターンの踏襲。単一ファイルか近接する数ファイル | `impl-l1` |
| L2 | 既存コードの読解が前提。複数ファイル横断。外部API連携。中規模リファクタ | `impl-l2` |
| L3 | アーキテクチャに触れる。トランザクション整合性、並行処理、状態機械、データ移行 | `impl-l3` |
| L4 | 仕様が曖昧で探索が必要。または L3 を2回試して失敗した | `impl-l4` |

判定に迷ったら**1段下げて試す**。失敗したら上げる。最初から上位モデルに投げない。

L3/L4 は境界が「難しさ」ではなく**セッションの長さと曖昧さ**。
1ファイル内で完結する難問は L3。文脈を長時間保つ必要があるものが L4。

### 4.3.1 視覚忠実度（V軸）

L0〜L4 はロジックの難易度を表す。**参照画像やデザインカンプを元にUIを作るタスクは、これとは別の軸で判定する。**
ロジックが L1 相当でも、視覚忠実度の要求が高ければ委譲先は変わる。

| V | 判定基準 | 委譲先 |
|---|---|---|
| V0 | 参照画像なし。既存コンポーネントの組み合わせ。見た目の要求が緩い | L軸に従う |
| V1 | ワイヤーフレームや粗いスケッチが参照。レイアウト構造が合っていればよい | `impl-l2` |
| V2 | LP画像・Figmaスクショ・既存サイトのキャプチャが参照。余白・字間・階層・配色を再現する。「こだわる」と言われた場合はここ | `impl-ui` |

**V2 の判定基準（1つでも該当したらV2）**
- ユーザーが参照画像を添付し、それに「寄せて」「近づけて」「再現して」と言っている
- ブランドサイト、LP、ポートフォリオなど、見た目そのものが成果物の価値である
- 複数画面をまたいでデザイントーンの一貫性が必要

**V2 タスクの進め方**
1. `impl-ui` はコードを書く前に、参照画像から**デザイントークンを抽出して文書化する**（`docs/design-tokens.md`、80行以内）。配色、タイポグラフィスケール、余白スケール、角丸、影、ブレークポイント。
2. トークン確定後の個別コンポーネント実装は、トークンに従うだけなので `impl-l1`（Sonnet）に委譲してよい。**高コストモデルを使うのは抽出フェーズであって、量産フェーズではない。**
3. 参照画像はタスクファイルにパスを記録する。再開時に同じ画像を見られるようにする。

### 4.4 Codex で使う場合

サブエージェントのモデル自動切替は行わない。代わりに:

- L0〜L2 のタスクは低コストモデルのセッションでまとめて消化する
- L3/L4 と設計・レビュー・監査は上位モデルのセッションで行う
- セッションを跨ぐので、`state.md` の更新規律が Claude Code 以上に重要になる

---

## 5. レビューゲート

### 5.1 判定

`reviewer` は各タスクの完了時に起動し、**`続行` か `NG` のどちらか一語**で判定する。中間の評価を出さない。

**`続行` の条件（すべて満たす）**
- タスクファイルの受入条件をすべて満たしている
- ビルドが通る / テストが通る
- `constraints.md` に違反していない
- 第7章のコーディング規約に違反していない
- 受入条件の範囲外の変更が混入していない

**`NG` の出力形式**（これ以外を書かない）
```
NG
理由: <1〜3行。何が満たされていないか>
修正指示: <具体的な指示。「改善してください」のような曖昧な指示を書かない>
```

### 5.2 NG3回ルール

- NGカウンタはタスク単位。`tasks/T-xxx.md` に記録する。
- `続行` が出た時点でカウンタをリセット。
- **同一タスクで NG が3回連続した時点で、reviewer は修正指示を出さずメインエージェントにエスカレーションする。**

エスカレーション時、reviewer は以下を返す:
```
ESCALATE
3回のNG理由: <各回1行>
根本原因の仮説: <1〜3行>
```

メインエージェントは以下のいずれかを選び、`decisions.md` に1行で記録する:

1. **タスクを分割する** — 分解不足が原因のとき（最も多い）
2. **難易度を1段上げる** — モデル能力が不足しているとき
3. **設計を見直す** — 実装が困難＝設計が悪い可能性。`architecture.md` に戻る
4. **受入条件を修正する** — 条件そのものが矛盾している、または過剰なとき
5. **ユーザーに判断を仰ぐ** — 仕様上のトレードオフが絡み、こちらで決められないとき

同一タスクで2回目のエスカレーションが起きた場合、**必ず5番（ユーザーに判断を仰ぐ）を選ぶ**。

### 5.3 監査ゲート

`constraints.md` に機微データの扱いが含まれる場合、`reviewer` の `続行` の後に `auditor` を起動する。

対象: 認証・認可、決済、個人情報の保存/送信、外部公開API、ファイルアップロード、データ移行

`auditor` は以下を出力する:
```
監査結果: 問題なし / 要修正 / 要人間判断
指摘: <あれば箇条書き>
根拠URL: <法令・ガイドラインを参照した場合は必ず記載>
```

**`auditor` は法令解釈の最終判断を下さない。** 法令・業界ガイドラインに関わる指摘は必ず一次情報を検索して根拠URLを付け、`要人間判断` としてユーザーに上げる。モデルの記憶だけで「問題なし」と判定することを禁止する。

---

### 5.4 視覚レビューゲート

V2 タスクでは、`reviewer` の `続行` の後に `ui-reviewer` を起動する。

`ui-reviewer` は参照画像と実装結果を並べて突合し、以下を出力する:
```
視覚判定: 続行 / NG
差異: <箇条書き。「余白が広い」ではなく「カード間の縦余白が参照24px相当に対し実装32px」のように具体値で>
```

- スクリーンショットを撮る手段（Playwright等）がある場合はそれを使う。無い場合は実装コードとトークン定義の照合に留め、**最終確認はユーザーの目視に委ねる**と明記して返す。
- NGカウンタは 5.2節と共通。ロジックのNGと視覚のNGを合算して3回でエスカレーションする。
- 「なんとなく違う」という判定を禁止する。差異を具体値で指摘できないなら `続行` を出す。

---

## 6. 状態管理と再開

### 6.1 再開手順

セッション開始時（再開モード）、メインエージェントは以下を**この順で**読む。読み終えるまで作業を始めない。

1. `.harness/state.md`（必ず全文）
2. `.harness/backlog.md`（必ず全文）
3. `docs/constraints.md`（必ず全文）
4. 直近のPRまたはコミット5件（`git log --oneline -5`、PRがあれば `gh pr list --limit 3`）
5. `state.md` が指す進行中タスクの `tasks/T-xxx.md`（あれば）

`docs/product.md` `architecture.md` は必要になった時だけ読む。毎回読まない。

読了後、**現在地を3行でユーザーに報告してから**作業を再開する。

### 6.2 各ファイルの書式と上限

**`.harness/state.md`（80行以内・毎回書き換え）**
```markdown
# 現在地
更新: 2026-09-14

## 進行中
T-014 ユーザー認証のセッション管理 (L3 / NG 1回)

## 直前に完了
T-013 ログインフォーム (続行)

## 次にやる
T-015 パスワードリセット

## 詰まっている点
なし
```

**`.harness/decisions.md`（1決定=1行・追記のみ）**
```
2026-09-14 | 認証 | セッションはRedisに保持。JWTは使わない。失効の即時性を優先 | T-014
```
200行を超えたら古い順に `archive/decisions-YYYYMM.md` へ退避し、本体は直近のみ残す。

**`.harness/backlog.md`（1タスク=1行）**
```
T-014 | L3 | 進行中 | ユーザー認証のセッション管理
T-015 | L1 | 未着手 | パスワードリセット
```

**`.harness/tasks/T-xxx.md`（150行以内）**
```markdown
# T-014 ユーザー認証のセッション管理
難易度: L3 / 担当: impl-l3 / NGカウンタ: 1

## 受入条件
- [ ] セッションIDが推測不可能
- [ ] 30分無操作で失効する
- [x] 同時ログイン数を制限できる

## レビュー履歴
1. NG - セッションIDにタイムスタンプが含まれ推測可能
```
完了後は `archive/tasks/` へ移動する。`tasks/` に置くのは進行中と次の1件のみ。

### 6.3 肥大化の防止

- どのファイルも上限を超えたら**要約して archive へ退避**する。上限を緩めない。
- `state.md` は追記しない。常に書き換える。履歴は `decisions.md` とコミットログが持つ。
- 経緯の詳細はファイルに書かず、**PR本文とコミットメッセージに書く**。
- 1タスク = 1コミット（またはPR）。コミットメッセージ1行目に `T-014` を含める。
  PR運用する場合、PR本文に「何を」「なぜ」「受入条件の充足」を各1〜3行で書く。

---

## 7. コーディング規約

### 7.1 コメント

- **原則1行。最大3行。** 5行のコメントブロックは書かない。
- **「何を」ではなく「なぜ」を書く。** コードを読めばわかることを書かない。
- ファイル冒頭の説明ヘッダは書かない。
- 自明な関数にドキュメンテーションコメントを付けない。公開APIのみ、1〜2行で。
- TODO は `// TODO(T-014): 内容` の形式。タスクIDの無いTODOを禁止する。

```
悪い: // ユーザーIDを受け取ってユーザーを取得し、存在しない場合はnullを返す
良い: // 退会済みユーザーもここでは返す。フィルタは呼び出し側の責務
```

### 7.2 その他

- 受入条件の範囲外を変更しない。改善案は実装せず報告に留める。
- 既存の命名・構造に合わせる。好みで書き換えない。
- 例外を握りつぶさない。握りつぶすなら理由を1行コメントで残す。

---

## 8. 形態別プロファイル

フェーズ2で、該当する形態の項目を `architecture.md` に反映する。

- **Webアプリ** — 認証方式、状態管理、APIとの境界、環境変数の扱い、デプロイ手順
- **モバイル** — ビルド環境（macOSが無いならクラウドビルド前提で設計）、権限要求、ストア審査で引っかかる要素、オフライン時の挙動
- **CLI** — 終了コードの体系、標準出力と標準エラーの使い分け、パイプ互換、設定ファイルの探索順
- **ライブラリ/SDK** — 公開APIの範囲を最初に確定。内部実装を露出させない。破壊的変更の基準
- **バッチ/データ処理** — 冪等性、途中失敗時の再実行単位、進捗の可視化、データ量の想定上限
- **API単体** — エラーレスポンスの形式統一、バージョニング、レート制限、OpenAPI定義の維持
- **デスクトップ/拡張機能** — 自動更新、権限、配布経路

---

## 9. 禁止事項

- ヒアリング完了・承認前に実装を始めること
- メインエージェントが実装コードを書くこと
- `reviewer` に書き込み権限を与えること
- `auditor` が根拠URLなしで法令適合を「問題なし」と判定すること
- 難易度判定を飛ばして最初から上位モデルに委譲すること
- V2 タスクでデザイントークンを抽出せずにコンポーネント実装を始めること
- V2 の量産フェーズまで高コストモデルを使い続けること
- `state.md` を更新せずにタスクを完了扱いにすること
