# T-002 デザイントークン抽出
難易度: V2 / 担当: impl-ui / NGカウンタ: 0（続行）

## 目的
参照画像からトークンを抽出し、以降のセクション実装（impl-l1）が値を判断せずに済む状態を作る。

## 参照画像
- docs/reference/lp-reference.jpg

## 受入条件
- [x] `docs/design-tokens.md`（80行以内）に以下を記載
  - 配色: 背景（クリーム・ピンク・紺）、文字（墨・グレー）、アクセント（珊瑚のピンク）、タグ背景。すべて hex
  - タイポグラフィ: 見出し用セリフ体・本文用サンセリフ体のフォント名、サイズスケール（h1〜caption）、字間、行間
  - 余白スケール（4〜128px 相当の段階）、セクション上下余白
  - 角丸、影、罫線の色と太さ
  - ブレークポイント
  - セクション幅の最大値とグリッド（制作物 4列 → 2列 → 1列）
- [x] `src/app/globals.css` の `@theme` にトークンが転記され、`pnpm build` が通る
- [x] `src/app/layout.tsx` で next/font によりフォントを self-host し、CSS 変数で参照できる
- [x] コンポーネントは作らない（量産は T-004 以降）

## レビュー履歴
1. 続行 - コミット c8276f6。navy-card / coral 系は確度低。T-012 で目視合わせ
