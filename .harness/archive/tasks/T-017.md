# T-017 Nisoine 写真を横に広げ、左右をフェードアウト
難易度: L1 / 担当: impl-l1 / NGカウンタ: 0（続行）

## 目的
ユーザー依頼: Nisoine セクションの写真を「もう少し広く」し、「サイドをフェードアウトで薄く」する。
元画像 public/images/nisoine/hero-sunset.jpg は 1552×656 の横長なので、切り出し比率を広げるだけで足りる。

## 参照
- src/components/sections/Featured.tsx（右カラム: 写真 flex-1 + スマホ w-1/3）
- src/app/globals.css の @theme（--aspect-photo: 13 / 12）、docs/design-tokens.md
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [x] `@theme` の `--aspect-photo` を `3 / 2` に変更し、design-tokens.md の「写真比率」を更新
- [x] globals.css に `@utility fade-x { mask-image: linear-gradient(to right, transparent, black 12%, black 88%, transparent); }` を追加（Tailwind v4 の @utility。design-tokens.md に「写真の左右フェード 12%」を1行追記、80行以内）
- [x] Featured の写真コンテナに `fade-x` を適用。`object-cover` は維持し、被写体（ふたり）が中央付近に来る `object-position` はトークン外なので既定（center）のまま
- [x] スマホ（w-1/3・aspect-phone・self-center）は変更しない。写真が広がった分、右カラム内で写真とスマホの比率が変わるのは許容
- [x] lg 未満（縦積み）でも fade-x が効き、写真の左右が薄くなる
- [x] 追加（ユーザー指示）: lg 以上で写真を左に伸ばし、見出し「テキストでは足りない」の「な」の下あたりまで届かせる。実装は Featured を `lg:grid-cols-12` にし、文章側を `lg:col-start-1 lg:col-span-6 lg:z-10`、ビジュアル側（写真＋スマホ）を `lg:col-start-5 lg:col-span-8 lg:row-start-1`（同じ行に重ねる）にする。負のマージンは使わない。写真の左フェード（fade-x）で文字の下は背景色に溶け、文字の可読性を保つ。写真の `aspect-photo` は 3/2 のまま。lg 未満は従来どおり縦積み
- [x] 写真の左端が「テキストでは足りない」の8〜9文字目の下に来ること（1440px でスクリーンショット確認。±40px 許容）
- [x] 1440px と 375px で崩れない。`pnpm lint` / `pnpm build` / `pnpm test` が通る

## レビュー履歴
1. 続行 - コミット e3b8e4b + dcb11d3。文章列は col-span-6（5 では見出しが3行に折れる）
