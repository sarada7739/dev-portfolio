# T-001 リポジトリ初期化と Next.js + Tailwind 雛形
難易度: L0 / 担当: impl-l0 / NGカウンタ: 0

## 目的
以降のタスクが同じ土台で動けるよう、ビルドとlintが通る最小の雛形を作る。

## 受入条件
- [x] `git init` 済みで、初期コミットが1件ある（メッセージ1行目に `T-001` を含む）
- [x] `pnpm create next-app` 相当の構成（App Router / TypeScript / Tailwind v4 / ESLint / src ディレクトリ）
- [x] `src/app/page.tsx` は空のセクション骨組みのみ（`<main>` に見出し1つ）。デザインは入れない
- [x] `pnpm lint` と `pnpm build` がエラーなしで通る
- [x] `.gitignore` に `.env*.local` と `.open-next/` が含まれる
- [x] `.env.example` に architecture.md の環境変数4件がキー名のみで並ぶ
- [x] `prettier` が導入され `pnpm format` で実行できる
- [x] `docs/` `.harness/` `.claude/` `AGENTS.md` `CLAUDE.MD` が初期コミットに含まれる

## 参考
- docs/architecture.md「ディレクトリ方針」「環境変数」
- 現時点で git リポジトリではない（`git init` から始める）

## レビュー履歴
（なし）
2. 続行 - 範囲外ファイル削除後（コミット 00f8fca）
