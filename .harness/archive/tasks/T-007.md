# T-007 HOW I WORK セクション
難易度: L1（V2 の量産フェーズ）/ 担当: impl-l1 / NGカウンタ: 0（続行）

## 目的
紺地の「HOW I WORK」ブロック（参照画像 y=870〜1225px 付近）をトークンだけで再現する。

## 参照
- docs/reference/lp-reference.jpg の HOW I WORK ブロック
- docs/design-tokens.md（navy グラデ、navy-card、navy-border、on-navy、coral-line、display フォント）
- src/data/index.ts の `howIWork`（4件。marker / markerColor 付き）
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [x] `src/components/sections/HowIWork.tsx`: `id="how-i-work"`。背景は `bg-linear-to-b from-navy-light to-navy-deep`。上端に `coral-line` の 1px 罫線、下端にも同じ罫線
- [x] 星空: CSS のみで表現する（画像を使わない）。`radial-gradient` を複数重ねた擬似要素、または小さな `on-navy` の点を `opacity` 低めで数個配置。トークン外の値が必要なら `@theme` に `--background-image-stars` 等を追加してよい（design-tokens.md にも1行追記）
- [x] 見出しラベル「— HOW I WORK —」: SectionLabel を紺地で使えるよう、色を props で切替可能にする（既定は現行どおり。`tone="onNavy"` で文字 `on-navy-muted`、線 `navy-border`）
- [x] 見出し「AIを、ていねいに使う。」: `font-serif text-h1 text-on-navy` を基本に、「AI」だけ `font-display italic` で欧文セリフにする。文字列の分割はコンポーネント内で固定でよい（データ化しない）
- [x] カード4枚を縦積み（`gap-4`）。各カード: `bg-navy-card border border-navy-border rounded-panel`、内側余白 `px-6 py-5`。左にマーカー、中央に title（`text-body font-bold text-on-navy`）と body（`text-small text-on-navy-muted`、`\n` → `<br>`）、右端に label（`text-caption tracking-wide text-on-navy-muted`）
- [x] マーカー: `marker` に応じて circle / triangle / square / dot を CSS で描く（12px 相当 = `size-3`、triangle は border トリック or インライン SVG）。`markerColor` の coral / amber / green / sage は `@theme` に `--color-marker-amber` `--color-marker-green` `--color-marker-sage` を追加して対応（coral は既存 `coral`）。design-tokens.md にも追記（80行以内）
- [x] `src/app/page.tsx` の Works の直後に HowIWork を追加
- [x] 1280px と 375px で崩れない。375px では label をカード下段に回り込ませてよい
- [x] `pnpm lint` と `pnpm build` が通る

## レビュー履歴
1. NG - 星空レイヤー（-z-10）がセクション背景の下に沈み描画されない。section に isolate が必要
2. 続行 - コミット 1eb8853 + 9c604fd（isolate 追加）
