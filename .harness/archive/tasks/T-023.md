# T-023 制作物カード（button 化）の中身が縦中央に寄る不具合
難易度: L0 / 担当: impl-l0 / NGカウンタ: 0（続行）

## 原因
T-022 で `<a>` → `<button>` にした結果、button 既定の「内容を垂直中央揃え」が効き、
グリッドで行高が揃うと説明文の短いカードのサムネイルが下がって上に余白が出る（本番で確認。ユーザー指摘）。

## 受入条件
- [x] `src/components/ui/WorkCard.tsx` の `<button>` の className にある `block` を `flex flex-col` に置き換え（`<a>` 側も同じにして両者の見た目を揃える）。中身は上詰め（justify は既定の start）
- [x] サムネイル・題名・説明・年号の順序と余白は変えない
- [x] `pnpm lint` / `pnpm build` / `pnpm test` が通る
- [x] 1440px で1段目4枚のサムネイル上端が同じ高さ（カード上端に接する）であることをスクリーンショットで確認（`docs/reference/shots/t023-row.png`）

## レビュー履歴
1. 続行 - コミット 7827fa9。8枚すべて img.top−card.top=1px を実測
