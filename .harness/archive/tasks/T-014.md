# T-014 プライバシーポリシーページ /privacy
難易度: L1 / 担当: impl-l1 / NGカウンタ: 0（続行）

## 目的
監査（T-009）で「要人間判断」となった外国第三者提供（Resend）と Turnstile のアクセス情報送信について、
ユーザー判断により /privacy ページにのみ詳細を記載する。フォーム直下の文言は変えず、リンクだけ足す。

## 参照
- docs/constraints.md「法令・表示」
- archive/tasks/T-009.md のレビュー履歴（監査指摘の要点）
- 根拠: https://resend.com/security/gdpr（米国保存・30日保持・DPA/SCC）、https://www.cloudflare.com/turnstile-privacy-policy/
- 既存: src/components/sections/{Header,Footer}.tsx、src/components/ui/ContactForm.tsx、src/data/site.json
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [x] `src/app/privacy/page.tsx`（Server Component）: `bg-cream`、Container 内に `font-serif text-h2` の見出し「プライバシーポリシー」と本文。`metadata` で title「プライバシーポリシー」、`robots: { index: false }` は付けない（通常どおり索引可）
- [x] 本文の節（各 2〜4 行、`text-body text-ink`）: ①取得する情報（氏名・メールアドレス・本文）②利用目的（返信のためのみ。それ以外に使わない）③外部サービスへの提供: 「送信内容は、メール配信のため米国の Resend, Inc. のサーバーを経由し、同社のポリシーにより最大30日間保存されます。同社は GDPR に準拠した DPA・SCC を公表しています」④スパム対策: 「Cloudflare Turnstile を利用しており、IP アドレス・ブラウザ情報等のアクセス情報が Cloudflare, Inc. に送信されます」⑤保存期間: 「当サイトのサーバーでは送信内容を保存しません。受信したメールは返信完了後に削除します」⑥お問い合わせ先: `site.email`。⑦制定日 2026-09-17
- [x] 各外部サービスの節に、上記根拠 URL への外部リンク（`rel="noopener noreferrer" target="_blank"`、`text-small underline underline-offset-4`）
- [x] ページ最下部にトップへ戻るリンク（`/`）。Header と Footer は再利用せず、シンプルな1ページでよい（Footer のフォームを含めない）
- [x] `src/components/ui/ContactForm.tsx` のフォーム直下の1行の末尾に「詳しくは<a href="/privacy">プライバシーポリシー</a>をご覧ください。」を追加（next/link 可。文言はそれ以外変更しない）
- [x] `docs/architecture.md`「フォームの境界」の「送信内容を一切保存せず」を「自サーバーでは保存しない（Resend 側では最大30日保持）」に修正
- [x] `pnpm lint` / `pnpm build` / `pnpm test` が通る。`pnpm build` の出力に `/privacy` が現れる

## レビュー履歴
1. 続行 - コミット 00da5d5
