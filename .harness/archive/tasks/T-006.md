# T-006 その他の制作物グリッド
難易度: L1（V2 の量産フェーズ）/ 担当: impl-l1 / NGカウンタ: 0（続行）

## 目的
「— その他の制作物 —」ラベルと 8 枚のカードグリッド（参照画像 y=490〜860px 付近）をトークンだけで再現する。

## 参照
- docs/reference/lp-reference.jpg の制作物グリッド
- docs/design-tokens.md「余白・レイアウト」のグリッド指定（4列 lg → 2列 sm → 1列、間隔 20px、サムネイル 5:3）
- src/data/index.ts の `works`（8件）
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [x] `src/components/ui/WorkCard.tsx`: `bg-card border border-line rounded-card shadow-card`。上にサムネイル（next/image、5:3、`object-cover`、角丸はカード上辺に合わせる）、下に title（`text-h3 font-serif text-ink-soft`）、description（`text-small text-gray`）、year（`text-caption tracking-wide text-gray-light`）。全体を `<a href={work.href}>` で包む
- [x] `src/components/sections/Works.tsx`: `id="works-all"`、`bg-cream`、上下余白 `py-section-sm lg:py-section`。SectionLabel「その他の制作物」→ グリッド。`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5`
- [x] サムネイルの `alt` は `${work.title} のサムネイル`
- [x] `src/app/page.tsx` の Featured の直後に Works を追加
- [x] 1280px で 4列、768px で 2列、375px で 1列。横スクロールが出ない
- [x] `pnpm lint` と `pnpm build` が通る

## レビュー履歴
1. 続行 - コミット 88db35c
