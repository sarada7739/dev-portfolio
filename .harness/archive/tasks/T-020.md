# T-020 伏せ字カードの演出（題名の伏せ部分を珊瑚色、説明を一部だけ見せる）
難易度: L1 / 担当: impl-l1 / NGカウンタ: 0（続行）

## 目的
T-019 の Steam ゲームカードに「伏せている」ことを匂わせる小さな演出を入れる。ユーザー「おまかせ」でメインが決定。

## 参照
- src/data/types.ts（Work）、src/data/works.json（1件目 wanwan）、src/components/ui/WorkCard.tsx
- docs/design-tokens.md（coral）
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [x] `Work` 型に任意項目 `titleMasked?: string` を追加。WorkCard は `titleMasked` があれば `title` の後ろに `<span className="text-coral">{titleMasked}</span>` を続けて描く（間にスペース1つ）
- [x] works.json の wanwan: `title` を "Wan Wan"、`titleMasked` を "〇〇〇"、`description` を "犬と、？？？？？？。" に変更。他の項目・他のカードは不変
- [x] `src/data/index.ts` の `works` は `satisfies Work[]` のままで型エラーにならない（任意項目なので他7件に追加不要）
- [x] `pnpm lint` / `pnpm build` / `pnpm test` が通る。1440px で1枚目の題名「Wan Wan 〇〇〇」の「〇〇〇」だけが珊瑚色（スクリーンショット `docs/reference/shots/t020-desktop.png`）

## レビュー履歴
1. 続行 - コミット bae5494（T-019 d6bb7e4 と合同レビュー）
