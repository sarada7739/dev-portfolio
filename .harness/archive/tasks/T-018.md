# T-018 Nisoine の CSS 製スマホを実機モックアップ画像に差し替え
難易度: L1 / 担当: impl-l1 / NGカウンタ: 0（続行）

## 目的
ユーザー提供のモックアップ画像（背景透過済み、359×714）で、CSS で描いていたスマホ枠＋screen-home.png を置き換える。

## 参照
- 素材: public/images/nisoine/phone-mockup.png（透過 PNG。docs/reference/nisoine/phone-mockup-cut.png と同一）
- src/components/sections/Featured.tsx（T-017 完了後の状態を前提。写真は 12 カラムグリッドで左に伸びている）
- src/data/featured.json（`screen` / `phoneFrame`）、src/data/types.ts
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [x] `featured.json` の `phoneFrame` を `/images/nisoine/phone-mockup.png` に変更。`screen` は残してよいが Featured では使わない
- [x] Featured の lg 以上・lg 未満の両方で、CSS のスマホ枠（`bg-button rounded-phone` 内に screen を入れる構造）を廃止し、`next/image` で `phoneFrame` を1枚描く。`alt="Nisoine のホーム画面（スマホ）"`。幅は lg 以上で従来と同じ `w-1/3 self-center`、lg 未満は写真の下に中央、幅は既定スケールの `w-48` 程度
- [x] `aspect-phone` と `rounded-phone` は画像に不要なので Featured から外す（トークン自体は残す。他で未使用なら design-tokens.md に「未使用」と1行注記、80行以内）
- [x] 画像の透過が効き、blush 背景に溶けていること（1440px スクリーンショットで確認）
- [x] 1440px と 375px で崩れない。`pnpm lint` / `pnpm build` / `pnpm test` が通る

## レビュー履歴
1. 続行 - コミット 2d682eb
