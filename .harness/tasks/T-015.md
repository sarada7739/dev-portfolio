# T-015 初回公開後の文言・リンク修正
難易度: L1 / 担当: impl-l1 / NGカウンタ: 0

## 目的
ユーザーからの修正依頼①②③⑤を反映する（④の Turnstile はユーザー側の環境変数修正で対応）。

## 参照
- src/data/{site.json, featured.json, how-i-work.json, types.ts}
- src/components/sections/{Hero, Featured, HowIWork}.tsx、src/components/ui/Button.tsx
- docs/design-tokens.md（coral / coral-text / coral-soft）
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [ ] ① `site.json` の `tagline` を「個人製作物、公開しているものを\nまとめております。\nぜひ、ご覧ください。」（3行）に変更。`SiteMeta` に `author: "Masato Goto"` を追加し、Hero でサブコピー（`sub`）の直後に `text-small tracking-wide text-ink-soft` で表示。display の3行が 375px でも孤立文字なし
- [ ] ② `featured.json` の `ctaLabel` を「はじめる」、`ctaHref` を `https://nisoine.com/` に変更。`Button` に `tone?: "dark" | "coral"` を追加（既定 dark。coral は `bg-coral text-on-navy`）。Featured のボタンは `tone="coral"`、外部リンクなので `target="_blank" rel="noopener noreferrer"`（Button に `external` prop を追加して対応）。Hero のボタンは変更なし
- [ ] ③ `how-i-work.json` 1件目の body を「エンジニアとしての経験とClaudeCode, Codexなど AIツールをすべての業務フローに組み込むことにより、\nアイデア出しから実装までを最短で行う。」に変更（`\n` は1か所）
- [ ] ⑤ HowIWork の見出しを「AIを、相棒に」に変更（「AI」のイタリック分割は維持。文字列はコンポーネント内固定のまま）
- [ ] `src/data/site.json` の `description`（meta）は変更しない
- [ ] `pnpm lint` / `pnpm build` / `pnpm test` が通る。1440px と 375px で崩れない

## レビュー履歴
（なし）
