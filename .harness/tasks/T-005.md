# T-005 Nisoine セクション
難易度: L1（V2 の量産フェーズ）/ 担当: impl-l1 / NGカウンタ: 0

## 目的
主力プロダクト Nisoine の紹介ブロック（参照画像 y=230〜480px 付近）をトークンだけで再現する。

## 参照
- docs/reference/lp-reference.jpg の Nisoine ブロック
- docs/design-tokens.md / src/app/globals.css の @theme
- src/data/index.ts の `featured`
- 実素材: public/images/nisoine/{hero-sunset.jpg, screen-home.png, phone-frame.png}
- 共通ルールは archive/tasks/T-004.md の「共通ルール」節と同じ

## 受入条件
- [ ] `src/components/ui/Tag.tsx`: ピル型タグ。`bg-coral-soft text-coral-text text-caption rounded-pill`、内側余白は 4px 12px 相当
- [ ] `src/components/sections/Featured.tsx`: `bg-blush`、下端に `blush-edge` の 1px 罫線。Container 内を lg 以上で 2 カラム（左: 文章、右: ビジュアル）、lg 未満は縦積み
- [ ] 左カラム: label を `text-label tracking-label text-coral`、heading を `font-serif text-h1 text-ink`（`\n` → `<br>`）、description を `text-small text-gray`、Tag を横並び折り返し（間隔 8px 相当）、Button「事例を読む →」
- [ ] 右カラム: 写真 `hero-sunset.jpg` を `rounded-card` で表示（比率 4:3 程度、`object-cover`）。その右に重ねてスマホ画面。スマホは **CSS で枠を描く**（`bg-button` の角丸 40px 相当の枠に `screen-home.png` を `rounded` で内包）。`phone-frame.png` は使わなくてよい（使う場合は画面領域の座標合わせが必要なので非推奨）
- [ ] スマホは写真の右下に重なり、lg 以上では写真の右端から少しはみ出す配置。lg 未満では写真の下に中央配置し重ねない
- [ ] 画像はすべて next/image、写真は `alt="海辺で夕日を眺めるふたり"`、スマホ画面は `alt="Nisoine のホーム画面"`
- [ ] `src/app/page.tsx` の SectionLabel「制作物」の直後に Featured を追加。`id="works"` の親は T-004 のまま
- [ ] デスクトップ（1280px）とモバイル（375px）で崩れない。横スクロールが出ない
- [ ] `pnpm lint` と `pnpm build` が通る

## レビュー履歴
（なし）
