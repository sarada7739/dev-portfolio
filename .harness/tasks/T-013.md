# T-013 Cloudflare 接続・環境変数設定・公開確認
難易度: L2 / 担当: メイン（運用作業）+ impl-l0（軽微修正）/ NGカウンタ: 0

## 目的
完成の定義③「Cloudflare Pages に公開され、独自URLでアクセスできる」と②「フォーム送信が自分のメールに届く」を満たす。

## 参照
- docs/architecture.md「デプロイ手順」「環境変数」
- archive/tasks/T-012.md の残差（375px ヘッダー 5px はみ出し）

## 受入条件
- [ ] Header の `h-nav` を `min-h-nav`（sm 以上は `sm:h-nav`）に変更し、375px でナビ行がヘッダー内に収まる（impl-l0）
- [ ] GitHub に `sarada7739/dev-portfolio`（Public）を作成し、main を push。CI（Actions）が緑
- [ ] Cloudflare Workers & Pages で GitHub 連携（ユーザー作業）。ビルド `pnpm exec opennextjs-cloudflare build`、デプロイ `pnpm exec opennextjs-cloudflare deploy`
- [ ] Resend アカウント作成 → API キー（ユーザー作業）。Turnstile ウィジェット作成 → Site Key / Secret（ユーザー作業）
- [ ] Cloudflare の環境変数に `RESEND_API_KEY`（Secret）`CONTACT_TO_EMAIL` `TURNSTILE_SECRET_KEY`（Secret）`NEXT_PUBLIC_TURNSTILE_SITE_KEY` を設定（ユーザー作業）
- [ ] 公開 URL でトップ・/privacy・/opengraph-image が表示される
- [ ] 公開 URL からフォーム送信 → `CONTACT_TO_EMAIL` にメール到達（ユーザーの受信確認）
- [ ] `src/data/site.json` の `url` を公開 URL に差し替えてコミット（impl-l0）

## レビュー履歴
（なし）
